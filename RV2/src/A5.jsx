// // File: src/components/Hobbies.js
// import React from "react";

// function A5() {
//   const age = 23;
//   const kkk = "nasr";
//   const interests = ["Reading", "Hiking", "Traveling"];

//   return (
//     <div>
//       <h1>
//         Hello my name is {kkk} and my age is {age}.
//       </h1>
//       <h2>My Interests:</h2>
//       <ul>
//         {interests.map((interest, idx) => (
//           <li key={idx}>{interest}</li>
//         ))}
//       </ul>
//     </div>
//   );
// }

// // // Following convention, component names should start with an uppercase character.
// // export default Hobbies;

// // // This code defines a React functional component called Hobbies This component renders a div` element that contains:

// // // A h1 heading that displays the name "nasr" (stored in the constant kkk) and their age (stored in the constant age). 2 A h2 heading that displays the "My Interests:".
// // // An unordered list (ul) that displays the interests of the person. The interests are stored in an array called interests and are mapped over using the map() method. For each interest, a list item (li) is created and the interest is displayed.
// // // The component does not take any props and does not have any state. It simply renders the hard-coded values and the interests array. The component can be used in a React application by importing it and including it in the JSX code.
